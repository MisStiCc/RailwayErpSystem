# 1. Перейди в папку проекта
cd /e/jetbra/RailwayErpSystem

# 2. Удали старый Jenkinsfile
rm Jenkinsfile

# 3. Создай новый с простым содержимым
cat > Jenkinsfile << 'EOF'
[вставь один из вариантов выше]
EOF

# 4. Закоммить и запушь
git add Jenkinsfile
git commit -m "Fix Jenkinsfile errors"
git push origin main